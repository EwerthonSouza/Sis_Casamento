/vendor/
composer.phar
config/central.local.php
scripts/*.log

# Uploads são estado do servidor (fotos enviadas por usuários + marcadores
# de cache de schema gerados por conexao.php), não código-fonte.
/uploads/*
!/uploads/.gitkeep

# Configuração local do Docker (cada máquina tem a sua)
.env
